{
  "name": "rakuai-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright E2E smoke tests for rakuai.com developer flow. Dev-only; not shipped to production.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
