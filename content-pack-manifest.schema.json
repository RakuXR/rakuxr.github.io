{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rakuai.com/schemas/content-pack-manifest.schema.json",
  "title": "Raku Content Pack Manifest",
  "description": "Schema for content pack manifest files. Full specification available with SDK access. Contact https://rakuai.com/contact.html",
  "type": "object",
  "required": ["pack_id", "template", "mode", "version"],
  "properties": {
    "pack_id": {"type": "string", "description": "Unique pack identifier."},
    "template": {"type": "string", "description": "Template family. See SDK docs for available templates."},
    "mode": {"type": "string", "enum": ["prototype", "presentation"]},
    "version": {"type": "string", "description": "Semantic version for the pack."}
  },
  "additionalProperties": true
}
