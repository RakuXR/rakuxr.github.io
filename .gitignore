.claude/
.venv/
.DS_Store
node_modules/
__pycache__/
*.pyc

# Local Node test artifacts (jsdom etc.) — static site has no node build
package.json
package-lock.json

# fly.dev migration scratch scripts (banned per fleet rules; may contain secrets)
.fly_*
